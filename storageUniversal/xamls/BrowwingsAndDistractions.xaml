<Page
    x:Class="storageUniversal.BrowwingsAndDistractions"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:data="using:storageUniversal.BorowwDb"
    xmlns:local="using:storageUniversal"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:muxc="using:Microsoft.UI.Xaml.Controls"
    xmlns:Windows10FallCreatorsUpdate="http://schemas.microsoft.com/winfx/2006/xaml/presentation?IsApiContractPresent(Windows.Foundation.UniversalApiContract, 5)"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid>
        <ListView Name="LandsTbl">
            <ListView.ItemTemplate>
                <DataTemplate  x:Name="dataTemp" x:DataType="data:Borrow">
                    <Grid>
                        <TextBlock Name="itemName" Text="{Binding ItemName, Mode=TwoWay}"></TextBlock>
                        <TextBox Name="amount" Text="{Binding Quantity}"></TextBox>
                        <TextBox Name="takenBy" Text="{Binding BorrowedBy}"></TextBox>
                    </Grid>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</Page>
